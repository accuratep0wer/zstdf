{
  "version": 1,
  "id": "demo-cp-v1",
  "domain": "cp",
  "require_wafer": true,
  "rules": [
    {"record": "MIR", "field": "LOT_ID", "required": true, "pattern": "LOT[0-9]+"},
    {"record": "MIR", "field": "JOB_NAM", "required": true},
    {"record": "MIR", "field": "JOB_REV", "required": true},
    {"record": "WIR", "field": "WAFER_ID", "required": true, "pattern": "[A-Z0-9]+"}
  ]
}
