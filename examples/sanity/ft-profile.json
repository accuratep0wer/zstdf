{
  "version": 1,
  "id": "demo-ft-v1",
  "domain": "ft",
  "require_wafer": false,
  "rules": [
    {"record": "MIR", "field": "LOT_ID", "required": true, "pattern": "LOT[0-9]+"},
    {"record": "MIR", "field": "JOB_NAM", "required": true},
    {"record": "PRR", "field": "PART_ID", "required": true, "pattern": "UNIT[0-9]+"}
  ]
}
